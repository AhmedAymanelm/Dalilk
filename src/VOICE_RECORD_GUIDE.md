# 🎤 دليل تسجيل الصوت من Terminal

## الطريقة 1: استخدام Python Script (موصى به) ⭐

```bash
cd "/Users/ahmed/Desktop/Graduation project/Ai/src"
source ../venv/bin/activate
python voice_record.py [session_id] [duration]
```

**مثال:**
```bash
# تسجيل لمدة 30 ثانية (افتراضي)
python voice_record.py

# تسجيل لمدة 20 ثانية مع session_id
python voice_record.py user123 20
```

## الطريقة 2: استخدام Bash Script

```bash
cd "/Users/ahmed/Desktop/Graduation project/Ai/src"
./voice_record.sh [session_id]
```

**مثال:**
```bash
./voice_record.sh test_session
```

## 📋 الخطوات:

1. **تشغيل السكريبت**
2. **اضغط Enter** للبدء في التسجيل
3. **تكلم** في الميكروفون
4. **اضغط Ctrl+C** للتوقف (أو انتظر انتهاء المدة)
5. السكريبت سيرسل الصوت للـ API تلقائياً
6. سيتم حفظ الرد الصوتي في ملف `.mp3`

## 📁 الملفات المحفوظة:

- `voice_response_YYYYMMDD_HHMMSS.mp3` - الصوت الرد

## ⚙️ المتطلبات:

- ✅ `ffmpeg` مثبت (موجود لديك)
- ✅ السيرفر شغال على `http://localhost:8000`
- ✅ `GEMINI_API_KEY` و `ELEVENLABS_API_KEY` في `.env`

## 🔧 تثبيت أدوات التسجيل (إذا لم تكن موجودة):

### macOS:
```bash
brew install sox      # أو
brew install ffmpeg   # (موجود لديك ✅)
```

### Linux:
```bash
sudo apt-get install alsa-utils
```

## 💡 نصائح:

- استخدم `session_id` مختلف لكل مستخدم للحفاظ على سياق المحادثة
- المدة الافتراضية 30 ثانية (يمكن تغييرها)
- الصوت المحفوظ يمكن تشغيله بأي مشغل صوتي

